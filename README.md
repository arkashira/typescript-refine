# Refactor with Validation

A tool to apply refactors and run type checks to ensure code safety.

## Usage

1. Install the tool using `poetry install`.
2. Run the tool using `python -m refactor refactor_command`.
3. The tool will apply the refactor and run a type check.
4. If the type check fails, the changes will be rolled back.
